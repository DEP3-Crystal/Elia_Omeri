//-Create an object which contains some information about a person who has influenced your life. 
//The object must contain: name (string), age (number) and qualities (array of strings).
//-Display in the console on separate lines (one console.log for each line) the person's information, like below:

let person = {
    name: 'John Doe',
    age: 50,
    qualities: ['Empathetic',
        'Honest',
        'Animal lover']
};
console.log("Name: " + person.name);
console.log("Age: " + person.age);
console.log("Qualities: " + person.qualities);
